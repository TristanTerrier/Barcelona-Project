import { Table } from './modules/Table.js'

const ProductsTable = new Table('http://localhost:3000', 'products')

// ProductsTable.findAll().then((data) => {

// })

// ProductsTable.findOneBy('id', 1)
